{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "runtime": "DOCKER",
    "containersPort": 3000,
    "healthCheckPort": 3000,
    "healthCheckPath": "/api/health"
  }
}
